# Tay Boy Interactive

One-page studio site. Static HTML, CSS, and JS with no build step.

- `index.html`, `style.css`, `main.js` — the page
- `assets/` — the print of Tay and the favicon

Open `index.html` directly or serve the folder with any static server.
